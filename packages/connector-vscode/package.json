{
  "name": "ares-connector-vscode",
  "displayName": "Ares Connector",
  "description": "Connects VS Code to Ares — seu tutor de IA residente",
  "version": "0.1.0",
  "publisher": "ares-ai",
  "engines": { "vscode": "^1.74.0" },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ares.connect",
        "title": "Ares: Conectar ao HUD"
      },
      {
        "command": "ares.disconnect",
        "title": "Ares: Desconectar"
      }
    ]
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --platform=node --format=cjs --sourcemap",
    "watch": "npm run build -- --watch",
    "package": "npm run build && npx @vscode/vsce package --no-dependencies -o ares-connector.vsix"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.74.0",
    "@types/ws": "^8.5.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
